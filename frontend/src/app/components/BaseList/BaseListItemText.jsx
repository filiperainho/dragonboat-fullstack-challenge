import React from "react";

import ListItemText from "@material-ui/core/ListItemText";

const BaseListItemText = (props) => {
  return (
    <ListItemText {...props} />
  );
};

export default BaseListItemText;
